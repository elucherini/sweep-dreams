# Supabase Configuration
SUPABASE_URL=<your url>
SUPABASE_KEY=<your key>
SUPABASE_TABLE=<your table>
SUPABASE_RPC_FUNCTION=schedules_near
SUPABASE_SUBSCRIPTION_TABLE=<subscription table>

# CORS Configuration (Backend/Render)
# Comma-separated list of allowed origins
# For production on Render, set to your Vercel domain(s)
# Example: CORS_ORIGINS=https://my-app.vercel.app,http://localhost:*,http://127.0.0.1:*
# Leave empty or set to "*" for development (allows all origins)
CORS_ORIGINS=http://localhost:*,http://127.0.0.1:*

# API URL (Frontend/Vercel)
# The backend API endpoint URL
# For Vercel deployment, set this in Vercel environment variables
# Example: API_URL=https://sweep-dreams.com
# For local development, defaults to http://localhost:8787 if not set
API_URL=http://localhost:8787

# Firebase service account JSON and project ID
FCM_SERVICE_ACCOUNT_JSON=<your_json>
FCM_PROJECT_ID=<your project id>
