# Copy this file to .env.mk and fill in your values
# .env.mk is gitignored

# iOS device name
DEVICE=YourDeviceName

# Android emulator name
ANDROID_EMULATOR=emulator-xxxx

# Backend hosts
API_URL_PROD=https://your-prod-api.workers.dev
API_URL_DEV=http://your-host:8787

# App Store Connect API credentials (for TestFlight uploads)
APP_STORE_API_KEY=...
APP_STORE_ISSUER_ID=...

# Web Push VAPID Key (Frontend/Flutter)
# The VAPID public key for Firebase web push notifications
# This is passed to Flutter via --dart-define=WEB_PUSH_CERTIFICATE_KEY_PAIR=...
# Generate your own key pair via Firebase Console or web-push library
WEB_PUSH_CERTIFICATE_KEY_PAIR=<your vapid public key>